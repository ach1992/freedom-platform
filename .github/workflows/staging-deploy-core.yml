name: Historical - Disabled Staging Core Deploy

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Disabled historical workflow
    if: ${{ github.repository == 'disabled/historical-workflow' }}
    runs-on: [self-hosted, Linux, X64, freedom-staging, php84]
    steps:
      - run: echo 'This historical deployment workflow used an obsolete fixed SHA and is disabled. See docs/development/staging-workflow-inventory.md.'
