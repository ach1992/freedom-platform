name: Historical - Disabled Staging Inventory

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Disabled historical workflow
    if: ${{ github.repository == 'disabled/historical-workflow' }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'This historical workflow is disabled. Use Staging Readiness and docs/development/staging-workflow-inventory.md.'
