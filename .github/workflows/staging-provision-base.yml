name: Historical - Disabled Staging Base Provision

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Disabled historical workflow
    if: ${{ github.repository == 'disabled/historical-workflow' }}
    runs-on: [self-hosted, Linux, X64, freedom-staging, php84]
    steps:
      - run: echo 'This historical root-level apt/aaPanel provisioning workflow is disabled. See docs/development/staging-workflow-inventory.md.'
