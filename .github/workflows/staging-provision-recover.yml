name: Historical - Disabled Staging Provision Recover

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Disabled historical workflow
    if: ${{ github.repository == 'disabled/historical-workflow' }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'This historical root-level recovery workflow is disabled. See docs/development/staging-workflow-inventory.md.'
