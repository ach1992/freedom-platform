name: Historical - Disabled Staging Runtime Gate

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Disabled historical workflow
    if: ${{ github.repository == 'disabled/historical-workflow' }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'This one-time remote runtime gate is disabled. Use Staging Readiness for current read-only facts.'
