name: Historical - Disabled Staging Site HTTP

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Disabled historical workflow
    if: ${{ github.repository == 'disabled/historical-workflow' }}
    runs-on: [self-hosted, Linux, X64, freedom-staging, php84]
    steps:
      - run: echo 'This historical aaPanel/OpenLiteSpeed mutation workflow is disabled. See docs/development/staging-workflow-inventory.md.'
