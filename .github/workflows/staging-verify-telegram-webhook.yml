name: Historical - Disabled Staging Telegram Webhook

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Disabled historical workflow
    if: ${{ github.repository == 'disabled/historical-workflow' }}
    runs-on: [self-hosted, Linux, X64, freedom-staging, php84]
    steps:
      - run: echo 'This historical real Telegram webhook mutation workflow is disabled. Reintroduce only with a current contract and owner-approved gate.'
